# Development-safe runtime defaults.
# Production must use HTTPS.
APP_ENV=development
BIND_ADDR=127.0.0.1:3000
# Dedicated loopback-only listener for local readiness and metrics scraping.
OPERATOR_BIND_ADDR=127.0.0.1:3001
PUBLIC_ORIGIN=http://127.0.0.1:3000
