# Copy to .env only when you need sqlx-cli (schema/query changes). Without a
# .env file, sqlx's query macros compile from the committed .sqlx/ offline
# cache and `cargo run` still works — see AGENTS.md.
DATABASE_URL=sqlite:data/app.db
BIND_ADDR=127.0.0.1:3000
