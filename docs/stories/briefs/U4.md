WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--u4 (Rust workspace, branch `rb-u4`, worktree of ~/dev/love/projects/ronit/rn-site, branched from `rebuild` which contains the kernel (K1–K3), server API (S1–S2) and rn-ui (U1)). Siblings U2 (/app) and U3 (/org) run at the same time; they own crates/app-member, crates/app-org and crates/server/src/api/q/{member,org}*. Do not touch other worktrees.

READ FIRST, in order: docs/rebuild/plan.md (§Product contract — platform operator, audience of one; §API); docs/kernel/index.html §Merge is first class (operator ruling with evidence), §Surfaces (/platform replaces the old /manage), §Placement (what the cluster page shows); crates/ui/src (Shell, Table, forms, Live, api; README for dev commands); crates/api/src; crates/server/src/api (S2's registration + `QueryDiff` pattern) and crates/server/src/ops (readyz raft status); crates/kernel/src/{merge,principal,cmd,resource,relation}/ public API; ~/dev/context/design/interface-taste.md (binding: dense operator console, status is a word in its own colour, never a pill). That is all the context you get.

YOU OWN: crates/app-platform/**, crates/server/src/api/q/platform*.rs (new files; additive registration lines), crates/server/src/api/cluster.rs (new), end2end/tests/platform.spec.ts, tools/seed.sh (a dev seeding script that registers a platform operator: the `platform:* #operator @person` relation is written by a kernel-provided bootstrap — use whatever K1 exposes for the first operator; if nothing exists, add `rn-site bootstrap-operator <email>` as a CLI subcommand in crates/server/src/main.rs (≤20 lines, dev + first-boot only, refuses if an operator already exists) and report it).

WORK — leg U4 platform bundle. Served only to `platform:* #operator`. End states, real data, Live where rows change:
1. `/platform` parties: Table of all parties (kind, display, status, created) with drill-in → identities, memberships, owned resources, relations as subject; Disable/Enable.
2. `/platform/identities`: identities (source, home zone, person, status, factor kinds — never secrets, never full emails: show the verified flag and a masked local part); drill-in → sessions (revoke), match candidates.
3. `/platform/sessions`: live sessions across the platform (identity, acting_as, created, last seen, expires), revoke; counts in the title.
4. `/platform/audit`: the whole audit log, Live tail, filter by command / actor, offset shown in mono; a row expands to the event payload.
5. `/platform/matches`: match candidates (identities, signal, score, status); RuleMatch with a required evidence field (refused empty); reject; the merged result shows person_link rows and the alias; Split.
6. `/platform/resources`: the resource registry (kind, public id, owner, home zone, status), relations per resource (object rows) with Revoke; Transfer.
7. `/platform/cluster`: node, version, raft membership and leader per group (sqlite/cache) from a new `GET /api/q/cluster` that reads hiqlite's metrics/is_leader (S1's readyz already probes both groups — extend that seam), feed head offset, subscriber count, observation-drain lag; no charts, a dense table.
8. Server-side: platform queries as `ReadStore` handlers guarded by the operator relation re-read per request; `QueryDiff` impls; `explain_` tests; matrix rows (member/org admins decline every platform query).
9. e2e (`end2end/tests/platform.spec.ts`): seed an operator via tools/seed.sh → two other contexts register with the same verified email pattern the match scanner keys on → candidate appears in /platform/matches live → RuleMatch with evidence → both identities now show one person → audit shows the ruling with evidence → revoke one of their sessions from /platform/sessions → that context lands on /auth.
10. Visual review (agent-browser): every /platform route at 1440 and 390, both themes — look at them.

OUT OF SCOPE: /app and /org pages, kernel or rn-ui changes beyond the bootstrap seam noted above.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-platform --all-targets --target wasm32-unknown-unknown -- -D warnings`; `cargo clippy -p rn-site --all-targets -- -D warnings`; `cargo test -p rn-site`; `trunk build --release` in crates/app-platform; `tools/size-gate.sh`; `bash -n tools/seed.sh`; playwright platform spec green; screenshots viewed; `pgrep -f rn-site` empty; no residue.
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-u4` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, queries added, the operator bootstrap mechanism, e2e steps passed, wasm size, screenshot paths and what you saw, gaps, blockers.
