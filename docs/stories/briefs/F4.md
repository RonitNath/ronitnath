WORKSPACE: /Users/ronitnath/dev/love/projects/ronit/rn-site (the MAIN checkout, branch `rebuild`, at F3's last commit). You are the only worker touching crate source; a delivery leg may be editing Containerfile/deploy/README in another worktree — do not touch those files.

READ FIRST, in order: docs/rebuild/plan.md; F3's report notes in `git log --format=%B -12` (new commands: ActAs, RevokeLink, RemoveMember; ConfirmMatch by credentials; operator authority; `Named::touched` unification); crates/ui/src (Shell, Table, form, api, live); crates/app-member/src, crates/app-org/src, crates/app-platform/src (the pages that hand-roll tables or lack controls); crates/ui/README.md; ~/dev/context/design/interface-taste.md (binding); ~/dev/context/procedures/verification.md. That is all the context you get.

YOU OWN: crates/ui/**, crates/app-member/**, crates/app-org/**, crates/app-platform/**, end2end/tests/**. You may READ everything. Server/kernel changes only if a bundle needs a query field that does not exist — minimal, listed in the report.

WORK — leg F4 ui gaps + feature completion in the bundles. End states:
1. rn-ui: `Shell` gains a rail slot (rn-org's organization switcher moves into it, above the nav); `Column` gains a cell renderer (a state word rendered in its own colour — never a pill) and `Table` a row-action column (per-row controls); `TextArea` primitive with the same sync/debounce semantics as `TextField`; `Live::disconnect` (called by the org switcher); the api client maps 422 to `ApiError::Invalid(fields)` with per-field messages and every form shows them beside the field; app-member's local 422 reader deleted. rn-ui tests extended for each.
2. Pages migrated onto the primitives: every hand-rolled `<table class="tbl">` in the three bundles becomes `Table` with the row-action column; document bodies use `TextArea`; table status columns use the cell renderer.
3. Features F3 made possible are reachable in the UI: /app/merge confirms a match with the other identity's credentials; /app/invitations and /org/invitations revoke a link; /org/members removes a member and offers ActAs (act as the organization) with the shell showing the acting party; /platform/sessions revokes any session; /platform/parties disables/enables organizations and groups; /platform/resources revoke/transfer with operator authority; trailing-slash shells.
4. e2e (`--workers=1`, chromium at least): member — the two-self-registrations → propose → confirm-with-credentials → one person flow; org — RevokeLink, RemoveMember, ActAs (audit shows the org as actor); platform — revoke a stranger's session, disable an organization. Existing specs stay green.
5. Visual review (agent-browser): every changed page at 1440 and 390, both themes — look at them; interface-taste rules hold.
6. crates/ui/README.md: the disk-vs-embedded bundle note (dev serves `dist/` when present, else the embedded copy — rebuild the server after `trunk build`).

ACCEPTANCE: `cargo fmt --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo clippy -p rn-ui -p rn-app -p rn-org -p rn-platform --all-targets --target wasm32-unknown-unknown -- -D warnings`; `cargo test --workspace`; `trunk build --release --config crates/<bundle>/Trunk.toml` ×4 (report sizes); `tools/size-gate.sh`; all playwright specs green; screenshots viewed; `pgrep -f rn-site` empty; no residue.
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units and `git push origin rebuild` after each. `CARGO_BUILD_JOBS=6`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, rn-ui API additions, pages migrated, e2e cases added and their results, wasm sizes, screenshot paths and what you saw, server/kernel edits (if any), blockers.

ADDENDUM (D1's findings — server, minimal, yours):
7. `crates/server/src/config.rs`: read `RN_SITE__ID_KEY_FILE` (file wins over `RN_SITE__ID_KEY`, trailing whitespace trimmed before the 32-hex validation), mirroring `db::cluster::secret`; test both paths. This blocks the cutover.
8. First operator on a formed cluster: `bootstrap-operator` opens the db directly and cannot run beside a live voter. Add config `bootstrap_operator_email` (`RN_SITE__BOOTSTRAP_OPERATOR_EMAIL`): when set, the server on startup and on every `Registered` event checks whether that email's person exists and no operator exists yet, and runs `rn_kernel::bootstrap_operator` once (audited; refuses when an operator exists; logs once). Test: set the env, register that email, `platform:* #operator` row appears; a second operator is never created. Update `deploy/CUTOVER.md` §6 and `deploy/compose.yaml` (commented example) accordingly — these two files are D1's but D1 has finished; edit them minimally.
