WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--s3 (Rust workspace, branch `rb-s3`, worktree of ~/dev/love/projects/ronit/rn-site). Sibling legs run in other worktrees and own crates/server/src/api/**, crates/app-*; do not touch other worktrees.

READ FIRST, in order: docs/rebuild/plan.md (§KDR(D): the starscape is re-homed with its full interaction contract); crates/starscape/src (S1's bundle: renderer, loaders, track, tuning) and crates/starscape/Trunk.toml; `git show main:public/js/starscape-explorer.js`, `git show main:public/js/starscape-ui.js`, `git show main:public/js/mini-globe.js`, `git show main:src/starscape/lod_asset.rs`, `git show main:src/starscape/bridge.rs`, `git show main:src/main.rs` (the deep-zoom explorer, atlas dialog and byte-range `g12.bin` LOD serving being re-homed — read for behaviour, then write your own); `git show main:end2end/tests/starscape.spec.ts` (the full contract; S1 re-proved the base part in end2end/tests/starscape.spec.ts — extend it); crates/server/src/assets.rs (S1's asset router; `static/stars/lod/**` (49 MB) is deliberately on disk, not embedded); tools/starcat/build_star_lod.py; docs/design.md; ~/dev/context/design/interface-taste.md. That is all the context you get.

YOU OWN: crates/starscape/**, crates/server/src/assets.rs + crates/server/src/presence/** (additive), templates/landing.html, end2end/tests/starscape.spec.ts, static/stars/**. You may READ everything.

WORK — leg S3 starscape explorer. End states:
1. The deep-zoom explorer is back: the landing's zoom/atlas interaction (whatever the old spec tested: pointer/keyboard zoom into a region, the atlas dialog naming stars in view, escape/close, resume) implemented in the wasm bundle, no separate JS modules (one bundle, `data-trunk` assets only).
2. LOD tiles: `static/stars/lod/**` served from disk with HTTP byte-range support (206, `Accept-Ranges`, `Content-Range`, 416 on bad ranges), `no-cache, must-revalidate`, correct content type, never embedded; a test covers 200/206/416 and that the rust-embed set excludes `stars/lod`. In the release image the directory is copied into `/app/static/stars/lod` (edit only the `COPY static` line's neighbour in Containerfile if needed — otherwise leave the Containerfile to the delivery leg and say so).
3. Reduced motion, both themes, phone width all still hold; the interaction contract from the old spec is re-proved in playwright (extend S1's file; every old test case has a counterpart or a written reason it no longer applies).
4. Visual review (agent-browser): landing at 1440/390, both themes, zoomed state and atlas open — look at them.

OUT OF SCOPE: anything under /api, shells, kernel.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-starscape --all-targets --target wasm32-unknown-unknown -- -D warnings`; `cargo clippy -p rn-site --all-targets -- -D warnings`; `cargo test -p rn-site -p rn-starscape`; `trunk build --release --config crates/starscape/Trunk.toml` (report wasm size); `tools/size-gate.sh`; playwright starscape spec green; screenshots viewed; `pgrep -f rn-site` empty; no residue.
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-s3` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, the interaction contract table (old case → new case/reason), wasm size, screenshot paths and what you saw, blockers.
