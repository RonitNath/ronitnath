WORKSPACE: /Users/ronitnath/dev/love/projects/ronit/rn-site (the MAIN checkout, branch `rebuild`, currently at U3's merge). You are the only worker now; no sibling worktrees are active. Branches `rb-u4` and `rb-u2` are pushed and unmerged.

READ FIRST, in order: docs/rebuild/plan.md; crates/server/src/api/query/mod.rs and crates/server/src/sub/mod.rs on `rebuild` (U3: `Touch::{None,Keys,Set}`, params-aware `touched`, per-subscription `Params`), the same files on `rb-u4` (`Named::changed(&Committed, …)`, `Named::Platform(Platform)`, `Params.subject/before`) and on `rb-u2` (member queries); the findings list below. That is all the context you get.

YOU OWN: everything in the repo. You may READ everything.

WORK — leg F3 integration + gaps. End states:
1. `git merge rb-u4` then `git merge rb-u2` into `rebuild`, resolving the query/sub seams into ONE design: `Named::touched(&Committed, principal, key, params) -> Touch` is the single entry (U3's set-diffing kept; U4's offset-keyed audit tail kept by passing `Committed`; member queries from U2 folded in); `Params` is the union of all fields; the socket carries per-subscription params (U3). Every test from all three legs stays green (`cargo test -p rn-site`, incl. tests/{org,platform,member}.rs and the surface matrix). Commit the merges separately from the fixes.
2. Kernel/server gaps, fixed at the source with tests:
   a. The observation drain is never spawned: start `observe` (last_seen drain + `observe::Matches` scan) with `AppState`, bounded and shut down on exit; a server test proves `session.last_seen_at` advances after 5 min of clock (inject the clock or lower the interval in tests) and that two identities sharing a verified OIDC subject get a `match_candidate` proposed; `/platform/cluster` "Observations pending" falls back to zero.
   b. `RevokeSession`: a platform operator may revoke any session (audited with the target identity); members still only their own.
   c. `Disable`/`Enable` accept any party public id (person, organization, group) with the authority rule: self or platform operator for persons; org owner or platform operator for organizations/groups. Result renders under the right tag (server `api::party`).
   d. Invitation links get a public id kind `l_` in rn-api/kernel ids (link is a `Table`; make it `Public`) and a `RevokeLink` command (owner/admin of the container, or operator); org and member invitation pages use it.
   e. `RemoveMember` command (or `SetRole` with `role: none`) — admin removes a lower role from a group/org; never the last owner.
   f. `ActAs` command: switch `session.acting_as` to an organization the identity administers (or back to the person); whoami reflects it; org commands audit as the org when acting as it; `org-audit`'s `acting_as` arm becomes live.
   g. Platform operator authority: `Revoke` any relation and `Transfer` any resource, audited.
   h. Shells: `/app/`, `/org/`, `/platform/` trailing slash serve the shell.
   i. `platform-identities` repaints on `PersonMerged` (touch the identities of both persons via a query — `Touch::Set` is acceptable).
3. rn-ui gaps: `Shell` gains a rail slot (org switcher moves there in rn-org); `Column` gains a cell renderer so a state word renders in its own colour (never a pill); `TextArea` primitive with the same sync semantics (document body in rn-app/rn-org uses it); `Live::disconnect` (org switcher calls it). Bundles rebuilt; screenshots of the changed pages viewed at 1440/390.
4. e2e: extend org/platform/member specs for RevokeLink, RemoveMember, ActAs, operator revoke-any-session, disable an organization. All specs green with `--workers=1`.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy --workspace --all-targets -- -D warnings`; wasm clippy for the four bundle crates; `cargo test --workspace`; `trunk build --release --config crates/<bundle>/Trunk.toml` ×4; `tools/size-gate.sh`; all playwright specs green; `pgrep -f rn-site` empty; no residue.
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units and `git push origin rebuild` after each. `CARGO_BUILD_JOBS=8` (you are alone). Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: merge commits, the unified `touched` signature, each gap with its commit and test, new command names, screenshot paths viewed, blockers.

ADDENDUM (U2's gaps, binding):
   j. `ConfirmMatch` must be reachable from a browser. RULING: `ConfirmMatch` takes the other identity's credentials (`other_email`, `other_password`) and the kernel verifies them in-command (same argon2id path and dummy-verify timing as SignIn; no session is created, nothing lands in a cookie); the existing `other_session` arm may stay for API callers. The /app/merge page uses it; e2e: two self-registrations → candidate via propose-match → confirm with the other credentials → one person.
   k. Index `relation(granted_by)` (new migration) so "invitations I minted" seeks; EXPLAIN test.
   l. rn-ui api client: a 422 carries the server's per-field messages (`ApiError::Invalid(fields)`), used by every form; delete the local reader in app-member/src/api.rs.
   m. rn-ui Table: a row-action column (per-row controls) so rn-app/rn-org/rn-platform stop hand-rolling `<table class="tbl">`; migrate those pages.
   n. crates/ui/README.md: note that dev serves a bundle from disk only when `dist/` exists, else the embedded copy — rebuild the server after `trunk build` for release, or keep `dist/` present in dev.
