WORKSPACE: /Users/ronitnath/dev/love/projects/ronit/rn-site (the MAIN checkout, branch `rebuild`, at e90bf61 = main). You are the only worker; no sibling worktrees.

READ FIRST, in order: docs/rebuild/plan.md (§Product contract, §Model, §API, §Gate manifest — you extend all four); docs/kernel/index.html; crates/kernel/src/{ids,resource,relation,cmd/mod,cmd/sign_in,cmd/share,cmd/invite,cmd/claim_link,link,principal,bootstrap,dev}.rs and crates/kernel/migrations/*.sql (never edit an applied migration; add `6_oidc.sql`); crates/server/src/{auth/**,links.rs,api/cmd.rs,api/query/named.rs,shell.rs,config.rs,assets.rs}; crates/api/src (Command trait, `bindings!`, `ALL_COMMAND_NAMES`, PublicId kinds); templates/{base,auth,claim}.html; crates/server/tests/{surface,api,commands,auth*,dev_sign_in}.rs; ~/dev/context/procedures/engineering.md (§Security, §Validation); ~/dev/context/design/interface-taste.md. Then the specs, fetched, not from memory: OpenID Connect Core 1.0, Discovery 1.0, RP-Initiated Logout 1.0, Back-Channel Logout 1.0, RFC 6749, 7636, 7009, 8414, 9207, 9700. That is all the context you get.

YOU OWN: everything in the repo. Keep the OP in its own modules: `crates/kernel/src/oidc/**`, `crates/server/src/oidc/**`, `crates/api/src/oidc.rs`, `templates/consent.html`, `crates/server/tests/oidc/**`.

WORK — leg O1: the deployment is an OpenID Provider. Owner rulings, binding:
- The IdP is integrated into the monolith; every deployment (ronitnath.com, isoastra.com) is its own issuer with its own keys; nothing federates between deployments. ronitnath.com gets it first, to test with.
- Client registration is operator-only for now (the `platform` tier); the resource shape must allow an organization to own a client later.
- Access tokens are opaque, hashed at rest, and BOUND TO THE ISSUING BROWSER SESSION: revoking/ending the session kills every token minted under it. Sign-out fires back-channel logout to RPs that registered a URI.
- `sub` is pairwise BY SECTOR = the client's owning organization (for operator-registered clients, the deployment's platform organization / `platform:*`): one stable subject string per (sector, person), minted once, never rotated, ≤255 ASCII, resolved through person aliases after a merge (both old subs still resolve to the survivor). `sub` is never an identity id.
- Persons get a HANDLE (migration): unique, human-chosen, lowercase `[a-z0-9-]{3,32}`, changeable by a command (`set-handle`), chosen at register (a form field, prefilled from the email local part, must be unique) — it is `preferred_username`. Merge: the survivor keeps its handle; the absorbed handle becomes an alias that still resolves.
- Service principals: `client_credentials` for confidential clients mints a token whose principal is a `service` party owned by the client's owner; relations grant it authority like any subject. This is the first route that accepts the `service` kind.

End states:
1. Model + kernel (`6_oidc.sql`, ids, commands, tests):
   - `oidc_client` resource kind (`c_` public id prefix), owner = a party (platform org or an organization). Metadata per RFC 7591 shape even without dynamic registration: client_name, client_uri, logo_uri, redirect_uris (exact match; loopback `http://127.0.0.1:<any port>` exception per RFC 8252), post_logout_redirect_uris, backchannel_logout_uri, token_endpoint_auth_method ∈ {client_secret_basic, client_secret_post, private_key_jwt, none}, jwks (JSON, for private_key_jwt), grant_types ⊆ {authorization_code, refresh_token, client_credentials}, scopes allowed, `trusted` (first-party: consent skipped), `members_only` (refuse persons with no relation under the owner), secret hash (argon2id is wrong here — high entropy; SHA-256 constant-time), created/rotated at.
   - `oidc_subject` (sector party × person → sub string), `oidc_consent` = relation rows `client:X #authorized @person:Y` with scopes (a relation with attributes, or a small table referenced by the relation — pick the one that keeps `check()` one query), `oidc_code` (single-use, 60 s, PKCE S256 challenge, nonce, redirect_uri, scopes, session), `oidc_token` (access/refresh, hash, session_id NOT NULL for user tokens, service party for client_credentials, family id for refresh rotation, expires, revoked), `oidc_key` (RS256 keys with kid, active/retiring/retired, generated by command, private key encrypted with the id key or a dedicated `RN_SITE__OIDC_KEY[_FILE]` — choose, document).
   - Commands (each one txn with the audit row inside, events emitted): register-client, update-client, rotate-client-secret, delete-client, set-handle, authorize (consent + code mint — actor is the person), exchange-code, refresh-token, client-credentials, revoke-token, revoke-consent, rotate-signing-key, end-session (RP-initiated). Token issuance IS a command (raft commit is fine).
   - Session end (sign-out, revoke-session, disable) cascades: tokens revoked in the same txn; a `SessionEnded` event carries the RP client ids so the server can POST logout tokens off the request path (observation lane / spawned task, bounded, no retries beyond one).
   - Invariants tested: sub never equals any id; sub stable across merge (both directions) and split; tokens die with the session; a group never owns a client; `check()` remains one query; EXPLAIN tests on every new lookup (token by hash, code by hash, subject by (sector, person), consent by (client, person)).
2. Server (`crates/server/src/oidc/**`), all under the public origin `RN_SITE__PUBLIC_ORIGIN` (config, required in prod, default `http://127.0.0.1:3004` in dev) — `iss` is that origin exactly:
   - `GET /.well-known/openid-configuration` and `/.well-known/oauth-authorization-server`: issuer, authorization_endpoint, token_endpoint, userinfo_endpoint, jwks_uri, revocation_endpoint, end_session_endpoint, response_types_supported ["code"], response_modes_supported ["query"], grant_types_supported, subject_types_supported ["pairwise"], id_token_signing_alg_values_supported ["RS256"], scopes_supported ["openid","profile","email","offline_access"], claims_supported, token_endpoint_auth_methods_supported, code_challenge_methods_supported ["S256"], claims_parameter_supported false, request_parameter_supported false, request_uri_parameter_supported false, authorization_response_iss_parameter_supported true, backchannel_logout_supported true, backchannel_logout_session_supported true.
   - `GET /oidc/authorize`: validates client_id + redirect_uri FIRST (failure renders a page, never redirects); then scope (must contain openid), response_type=code, state, nonce, PKCE S256 REQUIRED for every client type, prompt ∈ none|login|consent|select_account with the spec errors (login_required, consent_required, interaction_required, account_selection_required) and NO UI for prompt=none, max_age vs the session's auth time (re-auth = the /auth page with `next`), login_hint prefill, id_token_hint checked against the session; unsupported request/request_uri → request_not_supported. Unauthenticated → `/auth?next=` round trip (existing `next.rs`). Consent page `templates/consent.html` (askama, a door: client name, scopes as plain words, Allow / Deny; trusted clients skip it). Success → 302 to redirect_uri with code, state, iss (RFC 9207). Errors → redirect with error, error_description, state.
   - `POST /oidc/token`: client auth per registered method (basic, post, private_key_jwt with jti replay protection, none); grant_type authorization_code (code single-use; PKCE verifier checked; reject a verifier when no challenge was stored — downgrade; redirect_uri identical), refresh_token (rotation, family revoked on reuse), client_credentials. Response: access_token, token_type Bearer, expires_in, id_token (RS256, claims iss sub aud exp iat auth_time nonce sid azp; at_hash optional — omit), refresh_token when offline_access consented; `Cache-Control: no-store`, `Pragma: no-cache`. Errors per RFC 6749 §5.2 with the right status (401 + WWW-Authenticate for bad client auth).
   - `GET|POST /oidc/userinfo`: bearer access token → sub, plus profile (name, preferred_username, updated_at) and email (email, email_verified) per consented scopes, and `groups` = names of memberships under the client's owning organization only (never anything outside that subtree). 401 with `WWW-Authenticate: Bearer error="invalid_token"` otherwise.
   - `GET /oidc/jwks`: active + retiring keys, `kid`, `use: sig`, `alg: RS256`; cacheable.
   - `POST /oidc/revoke` (RFC 7009): client-authenticated; 200 even for unknown; a refresh token revokes its family.
   - `GET|POST /oidc/end_session`: id_token_hint (validated as ours, may be expired), client_id, post_logout_redirect_uri (exact match against registered or no redirect), state; asks the user to confirm (a page) when the hint is absent or names another session; ends the session (command) → cascade + back-channel POSTs `logout_token` (JWT: iss aud iat exp jti events sid sub; nonce MUST NOT be present) to each client with a backchannel_logout_uri that had a token under that session.
   - Trust boundaries preserved: the bearer access token is accepted ONLY at /oidc/userinfo and /oidc/revoke (and nowhere under /api/*); the session cookie never satisfies a bearer endpoint; every write is a command; declines uniform; rate limit the token endpoint per client (simple in-process bucket) so a secret cannot be brute-forced.
   - Existing routes: `/auth/register` gains the handle field; whoami gains `handle`; sign-out/revoke-session cascade; `/platform` gets queries `oidc-clients`, `oidc-tokens` (by session), `/app` query `authorizations` (consents for the person). Pages for those are leg O2 — you expose the queries and commands only, but the platform bundle needs NO change to stay green.
3. Proof: an in-repo relying party (`crates/server/tests/oidc/rp.rs`: discovery → authorize → code → token → id_token validation against jwks → userinfo → refresh → revoke → end_session with back-channel receipt) run through axum-test; the negative space (wrong redirect, replayed code, missing/wrong PKCE, verifier without challenge, wrong client secret, bearer on /api, cookie on /oidc/userinfo, cross-sector groups leak, prompt=none without session, members_only refusal, expired code, token after session revoke, refresh reuse); surface matrix rows for every new route × six principals; a signing-key rotation test (old kid verifies during overlap, then does not); merge test (both subs resolve). Then the real thing: `oauth2-proxy` (install with `brew install oauth2-proxy` if absent) against `just run-dev` on 127.0.0.1:3004 with a client you register through the command API — a script `tools/oidc-rp.sh` that starts it, and a documented manual click-through you performed (screenshots in docs/review/o1/, viewed). Do not attempt Forgejo/Plane here.
4. Docs: plan.md §Model/§API/§Gate manifest extended (commands list = 28 + yours, id prefixes + `c_`, config keys); `docs/oidc.md`: what is implemented vs declared unsupported (claims param, request objects, encrypted tokens, implicit/hybrid, form_post, WebFinger, device flow, dynamic registration, JWT access tokens — each with the spec's word for it), the sub rule, the session-binding rule, key material and rotation, the RP setup recipe for oauth2-proxy.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo check --release -p rn-site`; `cargo test --workspace --locked`; `tools/size-gate.sh` (split modules; no new fails); benches unchanged or extended (`check` still one query); e2e specs still green (`tools/seed.sh` + `just e2e`) since register gained a field; oauth2-proxy click-through done and screenshotted; `pgrep -f 'rn-site|oauth2-proxy'` empty; no residue; no secrets in git.
RAILS: production/deploy out of scope. Never print or commit secrets or private keys (test keys generated at test time). Stage explicit files; never `git add -A`. Commit in units (migration+kernel, server endpoints, RP proof, docs) and `git push origin rebuild` after each. `CARGO_BUILD_JOBS=8`. Do not spawn agents. Work autonomously; do not stop to ask questions. Do not kill processes you did not start — check the PID's start time and command line first; a stray `rn-site` on :3004 is the owner's.
REPORT: commit hashes; the schema you added (table by table); commands added; the config keys; the spec items implemented vs declared unsupported; the oauth2-proxy outcome with screenshot paths; test counts; anything you could not do in bounded time, as a finding with a repro.
