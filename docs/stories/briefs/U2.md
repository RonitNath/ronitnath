WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--u2 (Rust workspace, branch `rb-u2`, worktree of ~/dev/love/projects/ronit/rn-site, branched from `rebuild` which contains the kernel (K1–K3), server API (S1–S2) and rn-ui (U1)). Siblings U3 (/org) and U4 (/platform) run at the same time; they own crates/app-org and crates/app-platform and their own query modules under crates/server/src/api/q/{org,platform}*. Do not touch other worktrees.

READ FIRST, in order: docs/rebuild/plan.md (§Product contract, §API); docs/kernel/index.html §Surfaces (the /app member tier: "what any signed-in person gets"), §Merge is first class (the self-link flow), §Two write lanes; crates/ui/src (the components you compose — Shell, Table, form fields with immediate sync, Live store, api client; read crates/ui/README.md for the dev commands); crates/api/src (wire types); crates/server/src/api (how S2 registers queries and the `QueryDiff` trait for live rows; follow its pattern exactly); crates/kernel/src/{cmd,relation,document,group,merge}/ public API; ~/dev/context/design/interface-taste.md (binding). That is all the context you get.

YOU OWN: crates/app-member/**, crates/server/src/api/q/member*.rs (new files only; register them in the query table with additive lines), end2end/tests/member.spec.ts. If a kernel or rn-ui function is missing, write the thinnest call site and list the gap in your report — do not edit crates/kernel or crates/ui.

WORK — leg U2 member bundle. End states, every page real data through /api/q + /api/cmd + /api/sub (Live where rows change under the user):
1. `/app` home: identity block (display, person if resolved), organizations with roles, recent audit of my own commands (Live).
2. `/app/identities`: my identities (source, home zone, factors: email verified/unverified, password set), AddFactor/RemoveFactor/VerifyEmail forms with field-level errors, Disable self.
3. `/app/sessions`: my sessions (created, last seen, expires, current marked), RevokeSession per row; revoking the current one signs out (full navigation to /auth).
4. `/app/groups`: groups I belong to with role; per group: members (contacts visible only through `contact` scoping — the visible_contacts query), invitations I may mint (Invite → shows the claim URL with a copy button), SetRole/Leave; CreateGroup (personal).
5. `/app/documents`: documents I own or hold a relation on (Live), CreateDocument; editor page: title/body sync immediately through EditDocument with the client's rev as the version tag (conflict → shown next to the field, re-fetch), PublishDocument as the only commit button, Share (subject: person by public id, or one of my groups; relation viewer/commenter/editor), Revoke, Transfer (to a person or org I administer).
6. `/app/invitations`: links I minted (expiry, claimed-by) — revoke.
7. `/app/merge`: proposed match candidates involving my identities (signal, score); ConfirmMatch by signing in to the other identity inline (the form posts to the kernel's self-link proof); the result shows the merged person and its identities; Split available on a merged person.
8. Server-side: the member queries above as `ReadStore` handlers with `QueryDiff` impls so `Live` subscriptions receive row diffs; each query has an `explain_` test under EXPLAIN QUERY PLAN and a matrix row (S2's generated matrix must stay green — a member query must decline non-owners).
9. e2e (`end2end/tests/member.spec.ts`, playwright against `cargo run -p rn-site` dev): register → create group → mint invite → second browser context registers and claims → first context sees the member appear live → create document → share with the group → second context sees it appear live → edit in second context → first sees the change → revoke session from first context's sessions page kills the second context's tab (it lands on /auth).
10. Visual review (agent-browser): every /app route at 1440 and 390, both themes — look at them; no eyebrows, pills, dots, quips; tables elide by priority at 390; no horizontal scroll.

OUT OF SCOPE: /org and /platform pages, kernel or rn-ui changes, new command kinds.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-app --all-targets --target wasm32-unknown-unknown -- -D warnings`; `cargo clippy -p rn-site --all-targets -- -D warnings`; `cargo test -p rn-site`; `trunk build --release` in crates/app-member (report wasm size); `tools/size-gate.sh`; playwright member spec green; screenshots viewed; `pgrep -f rn-site` empty at the end; no residue.
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-u2` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, queries added (names + diff strategy), e2e steps that passed, wasm size, screenshot paths and what you saw, kernel/ui gaps, blockers.
