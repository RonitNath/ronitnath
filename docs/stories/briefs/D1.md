WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--d1 (Rust workspace, branch `rb-d1`, worktree of ~/dev/love/projects/ronit/rn-site, branched from `rebuild` with every feature leg merged). No other legs are running. Podman/Docker may not be available on this Mac — check `which podman docker`; if neither, prove the Containerfile by reasoning + `hadolint`-free careful review and a `nix`-free dry run of every RUN line's shell you can execute locally (trunk builds, cargo build --release), and say exactly what was not executed.

READ FIRST, in order: docs/rebuild/plan.md (§Gate manifest release rows; §Release); Containerfile; .forgejo/workflows/deploy.yml; deploy/compose.yaml, deploy/provision.sh, deploy/verify-cluster.sh (assert the OLD contract — must be rewritten to the new `/readyz` shape `{status,node,version,raft:{sqlite,cache}}`, bundle asset paths `/app/pkg/…`, `/pkg/starscape/…`, no `/api/realtime`); crates/server/src/{ops,assets,config,db}; every `Trunk.toml`; README.md (a 10-line pointer now); docs/design.md; ~/dev/context/procedures/delivery.md and ~/dev/context/procedures/ha-service.md (the pipeline and replication doctrine this repo is the reference for). That is all the context you get.

YOU OWN: Containerfile, .forgejo/workflows/deploy.yml, deploy/**, README.md, docs/README-ops.md (new, if README would exceed ~120 lines), flake.nix (comments/pins only). You may READ everything; do not edit crate source — list needed fixes in the report.

WORK — leg D1 delivery. End states:
1. Containerfile builds the four bundles with `trunk build --release --config crates/<bundle>/Trunk.toml` (pinned trunk 0.21.14, wasm-bindgen-cli pinned to the workspace's wasm-bindgen version), then `cargo build --release -p rn-site` with the bundles' `dist/` present for rust-embed; runtime stage copies `/app/bin/rn-site`, `/app/static` (including `static/stars/lod`), keeps uid 9751, read-only rootfs, `LEPTOS_*` env removed, `RN_SITE__ADDR=0.0.0.0:3160`, `RN_SITE__STATIC_DIR=/app/static`, `RN_SITE__MODE=prod`. Cache mounts for cargo registry/target and trunk's tool cache.
2. `deploy/verify-cluster.sh` asserts the new readyz shape, version match, voters=3 per raft group, one 200 on `/`, `/tokens.css`, one bundle asset per tier (`HEAD /app/pkg/<name>` — derive names from trunk's output or accept any 200 under the prefix), and that `/api/realtime`, `/manage`, `/pkg/rn-site.wasm` return 404. `bash -n` clean, shellcheck-clean if shellcheck is installed.
3. `deploy/compose.yaml`: env for the new contract (add `RN_SITE__ID_KEY_FILE` → `/run/secrets/id_key`, mounted like the two hiqlite secrets; `deploy/provision.sh` generates it once on nexus alongside the hiqlite secrets, never rotates it automatically, and documents that rotating breaks every public URL). Drop stale `LEPTOS_*` env and healthcheck paths that no longer exist.
4. `.forgejo/workflows/deploy.yml` gates: fmt, clippy --workspace, `cargo test --workspace --locked`, wasm `cargo check` for every bundle crate, `tools/size-gate.sh`, `bash -n` on every script; the flake devshell must provide `trunk` (S1 changed it — verify). Release job unchanged except the verify script's new assertions.
5. README.md rewritten for the new tree: what it is, the tiers, the workspace map, dev commands (`cargo run -p rn-site`, `trunk serve` per bundle from crates/ui/README.md, `tools/cluster.sh`, `tools/perf/run.sh`, `tools/seed.sh`), the ops surface table, the release path, and the cutover note (fresh formation, state archived). No history, no banners.
6. Cutover runbook `deploy/CUTOVER.md`: exact steps to archive `/data/apps/rn-site/state` on all three nodes, provision the id key, form the new cluster fresh, verify, and roll back (restore archive + previous digest). This is documentation only — do not run any of it.

OUT OF SCOPE: running the deploy, touching production hosts, crate source.

ACCEPTANCE: `bash -n deploy/*.sh tools/*.sh`; the workflow YAML parses (`python3 -c 'import yaml,sys;yaml.safe_load(open(".forgejo/workflows/deploy.yml"))'`); if a container engine exists, `podman build` succeeds and the image serves `/readyz` in single-node dev mode with a temp state dir; otherwise the executed subset is listed; `tools/size-gate.sh` green; no residue.
RAILS: production out of scope; never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-d1` after each. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, what was executed vs reviewed-only, the verify-cluster assertions list, crate-source fixes needed, blockers.
