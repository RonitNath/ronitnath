WORKSPACE: /Users/ronitnath/dev/love/projects/ronit/rn-site (the MAIN checkout, branch `rebuild`, at 0321d83). You are the only worker; no sibling worktrees.

READ FIRST, in order: docs/rebuild/plan.md (§Product contract, §API, §Gate manifest); crates/server/src/config.rs; crates/server/src/auth/{mod,session,next}.rs; crates/server/src/bootstrap.rs (how RN_SITE__BOOTSTRAP_OPERATOR_EMAIL grants the operator); crates/kernel/src/cmd/sign_in.rs and crates/kernel/src/cmd/register.rs (how a session row and its audit row are written); crates/kernel/src/bootstrap*.rs / wherever `bootstrap_operator` lives; templates/auth.html; crates/server/tests/{routes,surface,auth*,bootstrap}.rs; tools/seed.sh; .gitignore (note: "the app itself reads no .env" — you are changing that, update the comment); ~/dev/context/design/interface-taste.md; ~/dev/context/procedures/engineering.md §Security. That is all the context you get.

YOU OWN: everything in the repo. Keep the change small and local.

WORK — leg F7 dev sign-in bypass. Owner's words: "an additional button below the login button which creates a real session as the highest permission user, and only compiles in on non-release and requires a 'dev' flag in the .env". End states:
1. Compile gate: everything for this feature lives behind `#[cfg(debug_assertions)]` (server route + handler + template flag + kernel helper). A release build contains none of it: prove with `cargo check --release -p rn-site` and a test/`rg` in the report showing the route is absent from the release router (the routes matrix test must assert `POST /auth/dev` is 404 when the feature is compiled out — write the assertion with the opposite cfg so it runs in both profiles).
2. Runtime gate: `RN_SITE__DEV=1` (config field `dev: bool`, default false). Load `.env` from the working directory at startup (dotenvy, debug builds only, silently absent = fine; existing process env wins over the file). Add `.env.example` with `RN_SITE__DEV=1` commented and explained. Refuse the bypass unless debug build AND `dev` AND `mode == dev` — a 404 (not 403) when any is missing, so the route is indistinguishable from absent.
3. The button: in templates/auth.html, directly below the "Sign in" commit button, inside the sign-in pane, a second form `POST /auth/dev` with one button labelled "Sign in as operator" — rendered only when the template flag is true. Quiet styling (not the primary `commit` class; the existing secondary/quiet button style if one exists, otherwise a plain `<button>` with the page's existing token styles). No explanatory copy, no eyebrow, no pill (interface-taste).
4. The session is real: the handler resolves the highest-permission party — the platform operator (`platform:* #operator` relation). If none exists yet: register a dev person (`dev-operator@example.invalid`, display "Dev operator", a random unusable password) through the real `Register` command and grant it with `rn_kernel::bootstrap_operator` (which refuses a second operator — respect it: if an operator exists, sign in as THAT operator's identity). Then open a session through the kernel — a debug-only kernel entry (e.g. `kernel::dev::sign_in_as(identity)`) that writes the session row and an audit row with its own action name (`dev-sign-in`, actor = that identity) in one txn, emitting the same `SignedIn`-shaped event so the presence/sub machinery sees it; reuse the cookie issuance in auth/session.rs (same `rn_session` cookie, same attributes). Redirect to `/platform`. The domain vocabulary tests (`ADMITTED_UNPRODUCED` etc. in crates/kernel/src/domain/tests.rs) must account for the new action name in both profiles.
5. Tests (server, axum-test): with dev on → POST /auth/dev sets the cookie, `/api/whoami` shows an identity that `check`s `platform:* #operator`, the audit tail shows `dev-sign-in`; second call reuses the same operator (no second person, no second operator relation); with `dev` false → 404; with `mode=prod` (id key set) → 404; GET /auth renders the button only when on. Kernel test for the helper's audit row.
6. Docs: README "Working on it" gets one line (`cp .env.example .env` → the button); justfile gets `run-dev` = `RN_SITE__DEV=1 cargo run -p rn-site` (leave `run` as is). tools/seed.sh stays (e2e still uses it).

ACCEPTANCE: `cargo fmt --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo check --release -p rn-site`; `cargo test --workspace`; `tools/size-gate.sh`; a screenshot of /auth with the button (agent-browser or playwright), viewed; `pgrep -f rn-site` empty; no residue; `.env` never committed.
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units and `git push origin rebuild` after each. `CARGO_BUILD_JOBS=8`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, the cfg/runtime gates as implemented, the kernel entry name and audit action, tests added, screenshot path, the release-absence proof, blockers.
