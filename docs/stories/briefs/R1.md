WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--r1 (Rust workspace, branch `rb-r1`, worktree of ~/dev/love/projects/ronit/rn-site at the final `rebuild`). You are the independent reviewer: you did not write this code. Read-only first; fixes only for what you can prove, each with a red/green test.

READ FIRST, in order: docs/rebuild/plan.md (the contract and the gate manifest — every row must be evidenced); docs/kernel/index.html (the model the code claims to implement); ~/dev/context/procedures/engineering.md (§Security, §Validation: default-deny matrix, negative space, never mock the thing under test, secrets canaries, resource/lifecycle matrix); ~/dev/context/design/interface-taste.md; docs/perf/*.md (P1's measurements); then the tree. That is all the context you get.

YOU OWN: docs/rebuild/review.md (new) and any file you fix. You may READ everything.

WORK — leg R1 adversarial review. End states:
1. `docs/rebuild/review.md`: for every gate-manifest row in plan.md, the exact command you ran and its result (pass/fail/not-runnable-here with reason). For every trust boundary in §Product contract, the attack you tried (bearer on /api, cookie on /links, forged `p_` ids, another tenant's `?org=`, replayed idempotency key with a different body, GET that writes, `Sec-Fetch-Site: cross-site` on a command, oversized bodies, a subscription flood, a 10⁴-group principal, a link claimed twice, a merged party's session, an operator disabling themselves, the last owner leaving) and what happened. For every deleted surface, the 404 and the `rg` proof. Size gates, dependency hygiene (`cargo tree -d`, unused direct deps), `unsafe` count, `unwrap()` outside tests, `todo!/unimplemented!`, `#[allow]` inventory with reasons.
2. Model conformance: the schema vs the report's §Schema (table by table, column by column — differences listed with the leg's stated reason or flagged); every status vocabulary has a producing command; product tables reference identity not person; groups never own; `check()` is one indexed query; events are command-emitted; audit inside the txn.
3. Findings ranked by severity; anything you fixed has a commit with a test that fails without the fix (say which). Anything you could not fix in bounded time is a finding with a repro, not a fix.
4. Visual conformance: agent-browser pass over `/`, `/auth`, `/links/<t>`, and three routes per tier at 1440/390 both themes — look at them; interface-taste violations listed with screenshot paths.
5. Update `docs/rebuild/plan.md` §Gate manifest with an "evidence" column pointing at review.md sections; remove any plan text that the code contradicts (no banners; fix the text).

ACCEPTANCE: `cargo fmt --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `tools/size-gate.sh`; review.md complete; no residue; `pgrep -f rn-site` empty.
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-r1` after each. `CARGO_BUILD_JOBS=8`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: the findings table (severity, file:line, repro, fixed?), gate rows with any FAIL, commit hashes, screenshot paths, blockers.

KNOWN FINDING TO VERIFY AND FIX (from F4): `revoke_session`, `disable`, `enable`, `add_factor`, `remove_factor`, `sign_out` bind `member()`'s middle value (the person) into `audit.acting_as`; after `ActAs` the acted-as party must be what lands there (`refs::actor`). Red/green test: act as an organization, run each, assert the audit row's acting_as is the organization.
