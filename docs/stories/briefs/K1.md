WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--k1 (Rust workspace, branch `rb-k1`, worktree of ~/dev/love/projects/ronit/rn-site). Other workers are active in sibling worktrees; do not touch them or the main checkout.

READ FIRST, in order: docs/rebuild/plan.md (contract; §Model and §API bind you); docs/kernel/index.html (read as text — §Party…§Merge, §Schema, §check(), §Commands, §Two write lanes, §Performance gates); crates/api/src (the wire types you must produce/consume); ~/dev/context/procedures/engineering.md §Validation (never mock the thing under test; resource/lifecycle matrix); `git show main:src/auth/store.rs` and `git show main:src/operations/db.rs` ONLY for how hiqlite 0.14 is called (params!, query_as, txn) — everything else there is dead. Verify the hiqlite 0.14 client API against ~/.cargo/registry/src/*/hiqlite-0.14*/src (txn semantics: a batch of statements, not interactive). That is all the context you get.

YOU OWN: crates/kernel/** (and its Cargo.toml). You may READ everything. You may add workspace dependencies in the root Cargo.toml `[workspace.dependencies]` only. If you need an api type changed, change it in crates/api and say so in the report.

WORK — leg K1 kernel core. End states:
1. `migrations/1_kernel.sql` (embedded, hiqlite migrate) creates exactly the report's schema for: party, identity, factor, person_link, person_alias, match_candidate, membership, session, link, resource, relation, audit — with the indexes the report names, FKs, partial unique indexes where the model says UNIQUE, CHECK constraints for every status vocabulary, `created_at` INTEGER unix seconds. Later legs add tables only by new migration files.
2. `ids`: type-prefixed encrypted public ids exactly as plan.md §Model (AES-128-ECB over `table_tag(8) ‖ rowid(8)`, base64url no pad, prefix per table; `PublicId` in rn-api is the wire form; kernel `Id<T>` is the internal newtype, never Serialize). Key = `IdKey` from 32 hex chars; forged/foreign tokens rejected without DB contact; tests: round-trip, tag mismatch rejected, wrong key rejected, prefix mismatch rejected.
3. `store`: a narrow `Sql` trait (execute, query, txn-batch with per-statement affected-row assertions) implemented for `hiqlite::Client` and for an in-process `rusqlite` connection (test/bench engine — the same SQLite, not a mock). `Store` (read+write) and `ReadStore` (a type that exposes only query methods; a test asserts by trait bound that it has no execute). Commands read first, then commit one guarded txn batch (WHERE guards + expected affected rows) and retry once on guard failure — document this as the interactive-transaction substitute.
4. Domain: party (person|organization|group|service), identity (source, home_zone, person_id?), factor (email + password only built; kinds passkey|oidc admitted by CHECK, no code), session (token 256-bit random, SHA-256 at rest, sliding expiry, `acting_as`), link (bearer, expiry, claimed_by). Password: argon2id PHC, dummy-verify on unknown email so declines are timing-uniform.
5. Commands (one module each, `kernel::cmd::<name>`): Register (person party + identity + email factor + password factor + session, atomic), SignIn, SignOut, RevokeSession, AddFactor, RemoveFactor, VerifyEmail, Disable, Enable. Each: typed args (from rn-api), authorization check inside, audit row inside the txn (`audit.id` IS the change-feed offset), typed `Event` out. Idempotency: `key` UNIQUE in audit; replay with the same body returns the original result, different body → `Conflict`. Errors: one `Decline` type for all unauthorized/not-found (uniform), separate `Invalid` for validation.
6. `principal`: `Principal = Member{identity, acting_as} | Bearer{link} | Anonymous`; `resolve(cookie_token) -> Principal` and `expand(principal) -> SubjectSet` (person, org, groups, authenticated, public — group/org tables come in K2, so build `SubjectSet` with those slots and fill what exists). Warm cache keyed by session digest (bounded LRU, invalidated by feed events touching that identity/person). last_seen is an observation: `observe::touch(session)` enqueues; a drain task writes at most once per 5 min per session. Expiry sweep: `sweep_sessions()` bounded batch.
7. `feed`: trait `Feed { append (inside the command txn = the audit row), notify(offset), subscribe() -> Stream<offset>, read(from_offset, limit) -> Vec<Event> }`; hiqlite impl uses listen/notify on the cache group for wake-ups and the audit table for durable reads. Consumers resume by offset.
8. Tests (fast, in-process rusqlite unless the case is about raft): every command's contract; proptests: id forgery rejection, session digest never equals token, register is atomic (inject a failing statement → no partial rows), status transitions only via commands. `explain_*` tests: session resolve, feed read, audit-by-key under `EXPLAIN QUERY PLAN` asserting the index. One integration test against a real single hiqlite node (temp dir, free ports) covering Register→SignIn→feed notify→SignOut.
9. `benches/` (criterion): principal resolution cold/warm, id encode/decode, Register commit on rusqlite and on a single hiqlite node. Print against the budgets (≤2 ms cold / 50 µs warm principal). Record numbers in the report.

OUT OF SCOPE: relations, resources, organizations, groups, invitations, documents, merge (K2/K3 own those modules and migrations 2/3); server/http; UI. Do not add them or stub them.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-kernel --all-targets -- -D warnings`; `cargo test -p rn-kernel`; `cargo bench -p rn-kernel -- --quick` runs; `tools/size-gate.sh`; no file >600 lines; no `unwrap()` outside tests; `rg -n 'account|capability|grant' crates/kernel` returns nothing but comments explaining a deliberate absence (prefer nothing). Leave no test residue (temp dirs removed).
RAILS: never print or commit secrets or keys; test keys are obviously fake constants. Stage explicit files; never `git add -A`. Commit in units and `git push origin rb-k1` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, test count and names of the proptests/explain tests, bench numbers vs budget (cold/warm principal, register commit rusqlite vs hiqlite), the `Sql` trait signature, any api-crate change, blockers.
