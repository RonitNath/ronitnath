WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--u1 (Rust workspace, branch `rb-u1`, worktree of ~/dev/love/projects/ronit/rn-site). Other workers are active in sibling worktrees; do not touch them or the main checkout.

READ FIRST, in order: docs/rebuild/plan.md (contract; §API and §Surfaces bind you); docs/kernel/index.html §Surfaces and the UI-mock frames described in §What the board shows; crates/api/src (wire types — the only data you render); docs/design.md (tokens, type, density); ~/dev/context/design/interface-taste.md (binding: no eyebrows, no status pills/dots, borderless, headings ≤1.6× body, dense operator console, no explanatory copy, tables are a real component, no save button — fields sync immediately, commit only for state transitions); ~/dev/context/design/shell-conventions.md §Responsive shell only. That is all the context you get.

YOU OWN: crates/ui/**, crates/app-member/**, crates/app-org/**, crates/app-platform/**. You may READ everything. Workspace-level dependency pins may be added to the root Cargo.toml `[workspace.dependencies]` only.

WORK — leg U1 ui + bundles. End states:
1. `crates/ui/tokens.css`: the OKLCH tokens from docs/design.md, both themes authored (never inverted), `color-scheme`, five type sizes only, radius 0.25rem, borderless defaults, tabular-nums for numbers. This is the one stylesheet every bundle and the askama pages load.
2. `rn-ui` (Leptos 0.8 CSR, wasm target): `api` client — `query::<T>(name, params)` (GET /api/q), `command::<T>(name, args)` (POST /api/cmd with a fresh uuid `key`, same-origin fetch, `Decline` mapped to a typed error), `whoami()`; `sub` — one reconnecting WebSocket to /api/sub with full-jitter backoff, offset resume, heartbeat watchdog, a `Live<T>` store that applies `DiffOp`s into a keyed signal map and exposes it as a reactive view; `shell` — the tier shell: rail (wide) / drawer (narrow, Escape closes, focus returns to trigger), scope switcher listing only the tiers in `whoami.tiers`, identity block, footer pinned to the bottom with theme toggle and sign-out (a form POST to /auth/sign-out); `table` — sort, filter, pagination, priority-elided columns on narrow viewports, row drill-in callback, generic over a row type; `form` — text/select/toggle fields that sync on blur or a 3 s debounce through a caller-supplied command, showing validation next to the field until resolved; a `commit` button variant for state transitions only; `decline` view (the uniform not-found/forbidden page inside the shell).
3. The three bundles: trunk `index.html` per crate (loads `/…/pkg/` assets and `tokens.css`), `main.rs` ≤ 60 lines mounting the shell with the tier's route table. Routes (empty pages rendering the shell + an h1, no lorem): member `/app` → home, /identities, /sessions, /groups, /documents, /invitations, /merge; org `/org` → overview, /members, /groups, /documents, /invitations, /audit; platform `/platform` → parties, /identities, /sessions, /audit, /matches, /resources, /cluster. Each bundle's router uses `leptos_router` with the tier prefix as base.
4. Dev fixture: `crates/ui/examples/fixture.rs` — a tiny axum bin (dev-dependency) serving `/api/whoami`, `/api/q/*` from JSON fixtures in `crates/ui/fixtures/` and echoing `/api/cmd/*`, with a static `/api/sub` that sends one Diff then heartbeats. `trunk serve --proxy-backend=http://127.0.0.1:3199/api` in a bundle crate renders the chrome against it. Document the two commands in crates/ui/README.md (≤20 lines).
5. Tests: `wasm-bindgen-test` or plain unit tests for the Live store diff application (put/del/resync ordering, out-of-order offsets rejected), table sorting/filter/pagination logic, form debounce state machine; `cargo test -p rn-ui` (native) green.
6. Visual review per ~/dev/context/procedures/verification.md: agent-browser screenshots of each bundle's home at 1440 and 390 wide, both themes, against the fixture server — look at them: no eyebrows, no pills, no dots, footer pinned, drawer works at 390, no horizontal scroll.

OUT OF SCOPE: real feature pages (U2–U4 fill the routes), server code, kernel. Do not add placeholder feature UI beyond the h1.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-ui -p rn-app -p rn-org -p rn-platform --all-targets --target wasm32-unknown-unknown -- -D warnings` (and native for the fixture example); `cargo test -p rn-ui`; `trunk build --release` in each bundle crate (report wasm sizes); `tools/size-gate.sh`; screenshots viewed; dist/ not committed.
RAILS: never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-u1` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, tests, wasm sizes per bundle, screenshot paths and what you saw, the public API of rn-ui (module list + main signatures), blockers.
