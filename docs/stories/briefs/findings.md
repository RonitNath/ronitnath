# Cross-leg findings routed to the fixup leg (F1)
- K3: merge does not repoint `resource.owner_party_id` absorbed→survivor. RULING (orchestrator): ownership follows the person — merge repoints owner_party_id; Split copies back via person_link.from_person_id. Fix in kernel/merge after K2 lands.
- K3: `factor_email_unique_idx` is UNIQUE(value) deployment-wide, contradicting source-scoped identities (imports). Defer: no import source exists in this cut; record in plan.md backlog; sign-in lookup stays single-row.
- K3: `merge::rule::make_operator` bootstrap should be superseded by a proper first-operator path (U4 adds `bootstrap-operator` CLI).
- S1: deep-zoom explorer + LOD not rebuilt → S3 leg. Containerfile broken for trunk; verify-cluster.sh asserts old contract → D1 leg.
- K3: K3 added a placeholder `2_relations.sql`; K2's file must replace it wholesale.
- U3 gaps → F3: Disable/Enable must accept organizations (cmd::disable decodes Person only); Revoke for invitations (links have no public id — give links a public id `l_` or a RevokeLink command); remove-another-member (Leave only self → `RemoveMember` or SetRole(none)); acting_as switching command (act-as organization) so org commands audit as the org; rn-ui: Shell rail slot for the org switcher, Column with controls, TextArea primitive, Live::disconnect.
- U4 gaps → F3: observation drain never spawned in the server (last_seen + observe::Matches) — spawn with AppState, test it drains; RevokeSession must let a platform operator revoke any session; Disable/Enable for orgs/groups; `/app/` `/org/` `/platform/` trailing slash 404; Column cell renderer (coloured state word); platform-identities live repaint on PersonMerged; operator authority for Revoke/Transfer (ruling: operator may revoke any relation and transfer any resource, audited).
