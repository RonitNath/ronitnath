WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--u3 (Rust workspace, branch `rb-u3`, worktree of ~/dev/love/projects/ronit/rn-site, branched from `rebuild` which contains the kernel (K1–K3), server API (S1–S2) and rn-ui (U1)). Siblings U2 (/app) and U4 (/platform) run at the same time; they own crates/app-member, crates/app-platform and crates/server/src/api/q/{member,platform}*. Do not touch other worktrees.

READ FIRST, in order: docs/rebuild/plan.md (§Product contract, §API tiers); docs/kernel/index.html §Party (organization = tenant and ownership boundary), §One relation store, §Surfaces (/org operator); crates/ui/src (Shell, Table, forms, Live, api; crates/ui/README.md for dev commands); crates/api/src; crates/server/src/api (S2's query registration + `QueryDiff` pattern — follow exactly); crates/kernel/src/{org,group,invite,relation,document,cmd}/ public API; ~/dev/context/design/interface-taste.md (binding; dense operator console). That is all the context you get.

YOU OWN: crates/app-org/**, crates/server/src/api/q/org*.rs (new files; additive registration lines), end2end/tests/org.spec.ts. If a kernel or rn-ui function is missing, write the thinnest call site and list the gap — do not edit crates/kernel or crates/ui.

WORK — leg U3 organization operator bundle. The bundle is served only to principals with role ≥ admin on some organization; an org switcher at the top of the rail when there is more than one. End states, real data, Live where rows change:
1. `/org` overview: the organization (display, owner, created, counts folded into titles: "Members 12", "Groups 4", "Documents 9"), Transfer ownership (owner only; to a person by public id) behind a small side affordance, Disable/Enable organization.
2. `/org/members`: memberships on the org party (party, role, since) as a Table (sort/filter/paginate); SetRole inline (select syncs immediately; last-owner demotion refused and shown next to the field); remove (Leave on behalf, admin only for members below their role).
3. `/org/groups`: groups owned by the org, CreateGroup; drill-in: members with roles, contact scoping (which persons have `contact` shared to this group), Invite links for the group with claim URL + copy button, revoke.
4. `/org/documents`: documents owned by the org (Live), CreateDocument (owner = org), Share/Revoke/Transfer to/from groups and persons, editor with immediate sync + Publish commit as in the member tier (reuse rn-ui form primitives; do not fork them).
5. `/org/invitations`: all links minted for the org's groups (expiry, claimed-by, minted-by), revoke.
6. `/org/audit`: audit rows whose acting_as is the org, paginated by offset, Live tail; filter by command.
7. Server-side: org queries as `ReadStore` handlers scoped by `acting_as`/org public id with authorization re-read per request (an org admin of A must decline everything of B — matrix rows), `QueryDiff` impls, `explain_` tests.
8. e2e (`end2end/tests/org.spec.ts`): register → CreateOrganization → /org shows → invite a second browser context into an org group → claim → member appears live → SetRole to admin → second context now sees /org → first context transfers ownership → first context's /org overview loses the Transfer affordance.
9. Visual review (agent-browser): every /org route at 1440 and 390, both themes — look at them.

OUT OF SCOPE: /app and /platform pages, kernel or rn-ui changes.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-org --all-targets --target wasm32-unknown-unknown -- -D warnings`; `cargo clippy -p rn-site --all-targets -- -D warnings`; `cargo test -p rn-site`; `trunk build --release` in crates/app-org; `tools/size-gate.sh`; playwright org spec green; screenshots viewed; `pgrep -f rn-site` empty; no residue.
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-u3` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, queries added, e2e steps passed, wasm size, screenshot paths and what you saw, gaps, blockers.
