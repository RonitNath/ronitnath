WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--s1 (Rust workspace, branch `rb-s1`, worktree of ~/dev/love/projects/ronit/rn-site). Other workers are active in sibling worktrees; do not touch them or the main checkout.

READ FIRST, in order: docs/rebuild/plan.md (contract; §KDR(D), §Workspace, §API shells/ops bind you); docs/kernel/index.html §Surfaces (askama + bundles); docs/design.md (visual authority for the landing); deploy/compose.yaml + deploy/provision.sh + deploy/verify-cluster.sh (the env-var contract you must keep: RN_SITE__MODE, RN_SITE__DB_PATH, RN_SITE_HQL_NODE_ID, RN_SITE_HQL_NODES `id@raft@api`, RN_SITE_HQL_SECRET_{RAFT,API}[_FILE], RN_SITE_HQL_ADDR_{RAFT,API}, RN_SITE_HQL_LEARNER_ONLY, RN_SITE_NODE, SOURCE_GIT_HASH); `git show main:src/operations/db.rs`, `git show main:src/operations/config.rs`, `git show main:src/operations/shutdown.rs`, `git show main:src/main.rs` for the contracts being replaced (read for behavior, then write your own — no verbatim copies); crates/starscape/legacy/** (the renderer being re-homed) and `git show main:end2end/tests/starscape.spec.ts` (its interaction contract); ~/dev/context/design/interface-taste.md (binding taste rules). That is all the context you get.

YOU OWN: crates/server/**, crates/starscape/**, templates/**, static/**, tools/cluster.sh, end2end/tests/starscape.spec.ts. You may READ everything. Workspace-level dependency pins may be added to the root Cargo.toml `[workspace.dependencies]` only.

WORK — leg S1 server + presence. End states:
1. `rn-site` binary (crates/server): axum 0.8 app composed from feature routers (`ops`, `presence`, `assets`); `main.rs` ≤200 lines. Config via the `config` crate with `RN_SITE__` env prefix + `config.toml`; `Mode dev|prod`; `id_key` (32 hex) required in prod, generated-and-logged-as-absent in dev. Telemetry: tracing-subscriber env filter, JSON in prod. Shutdown on SIGTERM/Ctrl-C drains the listener then closes hiqlite. Cluster bootstrap: single node in dev from `db_path`; 3-node from the env contract above with the same validation rules the old code enforced (odd ≥3, local id present, `id@raft@api`, secret from value or file); learner-only supported. Migration integrity check retained (embedded migrations from crates/kernel are applied — call `rn_kernel::migrations()` if K1 has landed it; if not yet present, apply an empty migration set and leave a `todo!()`-free seam: a `Migrations` value passed in).
2. Ops: `/healthz` (dependency-free 200), `/readyz` JSON `{status, node, version, raft: {sqlite, cache}}`, `/version`. Every response carries `X-RN-App-Version`; HTML/API `no-store`; static `no-cache, must-revalidate`. No `/metrics`.
3. Assets: `static/**` and each bundle's `dist/` embedded with rust-embed in release (`crates/<bundle>/dist` → `/app/pkg/`, `/org/pkg/`, `/platform/pkg/`, `/pkg/starscape/`), served from disk in dev; correct content types; 404 for unknown.
4. Presence: askama 0.16 templates — `templates/base.html`, `templates/landing.html` (`/`), plus `/links`, `/about` if the old landing had such sections (re-create only what the old site rendered at `/`; check `git show main:src/app.rs`). The landing mounts the starscape bundle as a background layer with a `prefers-reduced-motion` static fallback, the mini-globe/nearest-city readout, and the same interaction contract the old spec tested. Both themes (server-rendered `data-theme` default from `prefers-color-scheme`, toggle client-side per docs/design.md). Tokens: reference `crates/ui/tokens.css` if U1 has landed it, else create `static/tokens.css` from docs/design.md values and say so.
5. `crates/starscape` (rn-starscape): a trunk-built wasm bundle owning the renderer, assets loader (`/static/stars/bright.bin`, `named.json`, cities), track/tuning/telemetry; the legacy Leptos-island glue is rewritten as plain wasm-bindgen entry (`start()` mounts on `#starscape`). Delete `crates/starscape/legacy/` when done. `trunk build --release` succeeds; `cargo check --target wasm32-unknown-unknown -p rn-starscape` clean.
6. `tools/cluster.sh`: starts 3 local nodes (temp dirs, ports 8101/8201… , app 3161-3163, env per the contract, dev secrets), `status`, `kill <n>`, `stop`; used later by the perf and resilience legs.
7. Tests (crates/server/tests): ops routes; headers (version header on every route, cache-control split); assets 404 vs 200; landing renders both themes (askama output contains the expected mount and no `Generated`/build-machinery text); config validation (bad RN_SITE_HQL_NODES shapes fail closed); a `readyz` integration test against a real single hiqlite node. `end2end/tests/starscape.spec.ts` re-proves the interaction contract against `cargo run -p rn-site` (dev mode) and passes.
8. Visual review per ~/dev/context/procedures/verification.md: agent-browser screenshots of `/` at 1440 and 390 wide, both themes — look at them; the sky renders, no scrollbar, no eyebrows/quips.

OUT OF SCOPE: `/api/*`, auth pages, shells, kernel logic (S2 owns those after K1). Do not stub them.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-site -p rn-starscape --all-targets -- -D warnings`; `cargo test -p rn-site`; `trunk build --release` in crates/starscape; `tools/size-gate.sh`; `bash -n tools/cluster.sh`; `tools/cluster.sh start && tools/cluster.sh status` shows 3 ready nodes then `stop` leaves no processes (`pgrep -f rn-site` empty); playwright starscape spec green; screenshots viewed. Leave no residue (temp dirs, dist/ not committed).
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-s1` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, tests run, bundle size (wasm bytes), screenshot paths you viewed and what you saw, the exact env contract you preserved, blockers.
