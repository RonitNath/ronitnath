WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--s2 (Rust workspace, branch `rb-s2`, worktree of ~/dev/love/projects/ronit/rn-site, branched from `rebuild` which contains K1 kernel core, S1 server+presence, U1 ui+bundles). Sibling workers K2/K3 edit crates/kernel in other worktrees; you consume K1's kernel API only. Do not touch other worktrees.

READ FIRST, in order: docs/rebuild/plan.md (§Product contract trust boundaries, §API — binding, §Gate manifest rows route/auth matrix, negative space, GET never writes); docs/kernel/index.html §Surfaces, §API shape, §Live data; crates/server/src (S1's app composition, config, cluster bootstrap, assets, templates — extend, do not restructure); crates/kernel/src/{principal,cmd,feed,store}/ (what you call); crates/api/src (the wire types — you serialize exactly these); crates/ui/src/{api,sub}/ (what the bundles send — match it); ~/dev/context/procedures/engineering.md §Security (default-deny, matrix, Sec-Fetch-Site, secrets canaries); ~/dev/context/design/interface-taste.md (auth pages are human-visible). That is all the context you get.

YOU OWN: crates/server/src/{api,auth,shell,sub,links}/**, crates/server/tests/**, templates/{auth,claim,shell}*.html. You may READ everything. If a kernel function you need is missing, write the thinnest call site against K1's public API and list the gap in your report — do not edit crates/kernel.

WORK — leg S2 api + auth + shells. End states:
1. Extractors: `Session` (cookie `rn_session` → kernel `resolve` → `Principal::Member`), `Bearer` (path token on `/links/<token>` only), `Anonymous`. Structural separation: the `/api` router only accepts the cookie extractor; the `/links` router only the bearer; a compile-time-visible test that the api router has no bearer route and vice versa. Mutations require `Sec-Fetch-Site: same-origin|none` (never Origin equality).
2. `POST /api/cmd/<name>`: dispatch table generated from rn-api's `Command` trait (every name in §Product contract that K1 implements; names K2/K3 will implement return the uniform decline until they land — list them in the report, do not stub kernel logic). Body = `CommandEnvelope`; reply `CommandReply{offset,result}`; idempotency handled by the kernel; errors: `Invalid` → 422 with field errors, everything else → uniform decline bytes (403/404 identical body).
3. `GET /api/q/<name>`: handlers receive `ReadStore` only (K1's type). Queries for this leg: `whoami`, `sessions` (mine), `identities` (mine: sources, factors without secrets, verified flags), `audit` (mine, paginated by offset). `Cache-Control: private, no-store`.
4. `GET /api/whoami`: exactly the `Whoami` type; tiers computed from relations (`member` always for a session; `org` if any membership role ≥ admin on an organization; `platform` if `platform:* #operator @person`). Never emails, never internal ids — a test scans the JSON for `@` and for any 64-bit-looking integer.
5. `WS /api/sub`: per-connection subscription set; a feed subscriber task wakes on `Feed::subscribe()`, reads events from the last delivered offset, maps them to row diffs per subscribed query (a `QueryDiff` trait per query: given an event, which keys changed → re-read those rows → `DiffOp`s), sends `SubMessage::Diff`; heartbeat every 25 s; bounded per-connection queue → on overflow send `Resync` and drop the backlog; on release mismatch (`X-RN-App-Version` from the client's first message) send `Resync`. Global cap on connections per identity (e.g. 16) and per node; over cap → close with 1013.
6. Auth pages (askama, `templates/auth.html`): `GET /auth` (sign-in + register in one page, `next` param validated as same-origin path), `POST /auth/register`, `/auth/sign-in`, `/auth/sign-out`; cookie HttpOnly, Secure in prod, SameSite=Lax, path `/`; uniform decline with dummy-verify timing (kernel provides). Errors rendered next to the field. No copy beyond labels.
7. Shells: `GET /app{/*}`, `/org{/*}`, `/platform{/*}` serve `templates/shell.html` with the tier's bundle only if the principal holds the tier (plan.md §API: anonymous → 302 `/auth?next=`; authenticated-unauthorized → 404 for `/org`; `/platform` → 404 for everyone without it, including anonymous). The shell HTML is `no-store`; bundle assets `no-cache`.
8. Claim page: `GET /links/<token>` (askama) shows what the link grants and a sign-in/register-to-claim form; `POST /links/<token>/claim` runs `ClaimLink` with the bearer + the session (if K2's ClaimLink is not yet merged, the page renders and the POST returns the uniform decline — say so).
9. Tests (crates/server/tests, axum-test against a real single hiqlite node — reuse S1's harness): the full route × principal matrix (anonymous, member, org operator, platform operator, authenticated-unauthorized, bearer-on-api, cookie-on-links) for every route in the tree — generated from the router's route list so a new route without a matrix row fails the test; `negative_space`: `/manage`, `/api/realtime`, `/pkg/rn-site.js`, `/manage/*` → 404 and `rg` finds no `manage|capability|resource_grants` symbol; `read_store_has_no_execute`; `whoami_leaks_nothing`; `sub` integration: register in one client, subscribe in another, run a command, the diff arrives with the command's offset; overflow → Resync; secret canary: a sentinel password/token never appears in any log line captured by the test subscriber.
10. Visual review (agent-browser): `/auth` and `/links/<token>` at 1440 and 390, both themes — look at them.

OUT OF SCOPE: feature queries for documents/groups/orgs/matches (U2–U4 add their queries alongside their pages, against K2/K3), kernel edits, bundle code.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-site --all-targets -- -D warnings`; `cargo test -p rn-site`; `tools/size-gate.sh`; screenshots viewed; no test residue; `pgrep -f rn-site` empty at the end.
RAILS: production/deploy out of scope. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-s2` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, the matrix size (routes × principals) and its generation mechanism, the list of command names that return decline pending K2/K3, kernel API gaps, screenshot paths and what you saw, blockers.
