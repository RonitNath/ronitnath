WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--x0 (git worktree of ~/dev/love/projects/ronit/rn-site, branch `rb-x0`, at f0df878 on top of `rebuild`). Another worker (leg O1, the OpenID Provider) is committing to `rebuild` in the main checkout right now — you do not touch `crates/**` or `templates/**` at all in this leg; you read them. There is no prebuilt target/ here; you only need `cargo build -p rn-site` (debug) for the ephemeral script, run it once and let it take the time.

READ FIRST, in order: docs/stories/platform-admin.md (the input — 21 stories + rulings); docs/rebuild/plan.md; docs/kernel/index.html; ~/dev/context/procedures/orchestration.md and brief-template.md (you will write the leg briefs); ~/dev/context/resources/tldraw.md and ~/dev/context/tools/tldraw-board/** (the board template — copy, do not reinvent); ~/dev/context/design/interface-taste.md; then the code: crates/kernel/src (commands, principal, relation, bootstrap, dev), crates/server/src (config, api, auth, ops, shell), crates/app-platform/src (every existing platform page), tools/{cluster,seed,size-gate}.sh, justfile, .env.example. Briefs of the current in-flight OIDC leg: /private/tmp/claude-502/-Users-ronitnath-dev/1a80dd12-969c-454a-80ea-f8f4533b4fa6/scratchpad/briefs/O1.md (stories E14–15 are being delivered there; your requirements reference it, they do not redo it). That is all the context you get.

YOU OWN: docs/stories/**, docs/design/platform-admin/**, tools/ephemeral.sh, justfile (add recipes only), README.md (one paragraph). Nothing else.

WORK — leg X0: scope and design the platform-admin stories. End states:
1. `docs/stories/platform-admin-requirements.md` — for every story A1…G21: what exists today (with file:line evidence), what is missing, and the concrete feature requirements to close it: commands (name, actor rule, audit shape, events), queries, routes, screens (which /platform page, which controls), model changes (tables/columns/relations — e.g. a `product` enablement table read at request time with a cached projection invalidated by the feed; operator delegation as the existing `platform:* #operator` relation with grant/revoke commands; impersonation as a session whose `acting_as`-style field names the person, with the operator as actor in every audit row and a banner in the impersonated person's own view; signing-key rotation; backup/restore; org export/import; a `rn-site admin` CLI against the store), config keys, and the acceptance test each requirement needs (falsifiable). Mark each requirement: EXISTS / EXTEND / NEW / DEFER (with the reason). Runtime product enablement is binding: a platform screen toggles a product and its routes appear/vanish without a restart on every node (design the propagation through the feed).
2. Design in tldraw: copy `~/dev/context/tools/tldraw-board/` to `docs/design/platform-admin/`, write `src/board.ts` as a pure function with frames for: (a) the deployment topology + product-enablement propagation (sequence: toggle → command → feed → every node's router projection); (b) impersonation as a state machine (operator session → impersonating → back, with what ends it) and the sequence of a sign-in-as; (c) operator delegation (grant/revoke, first-operator bootstrap); (d) disable/enable cascade (sessions, links, tokens, consents); (e) backup/restore + export/import flows; (f) UI mocks: the deployment screen, the products screen, the person detail (everything the system knows), the audit with filters, the impersonation banner — at 1440 and 390. Prose goes in `public/index.html` (the report: the requirements table condensed, linking each frame). `pnpm install && npx tsc -p . && pnpm build`; export the `.tldr` beside it (`docs/design/platform-admin/platform-admin.tldr`); commit `dist/index.html` and the `.tldr`, not node_modules. Serve it (`python3 -m http.server 8765 --directory dist`), open it with agent-browser, LOOK at every frame at least once (screenshots into docs/design/platform-admin/shots/, viewed), fix overlaps/unreadable text, then stop the server. Never publish as a Claude artifact.
3. `tools/ephemeral.sh` — ephemeral single-node dev instance per worktree (story G20): derives a stable port set and state dir from the worktree path (hash → ports in 3300–3399 / 8300+ ranges, state under `$worktree/target/ephemeral/`), writes a private id key there, exports `RN_SITE__DEV=1`, dev mode, starts `target/debug/rn-site` (building if absent), waits on /readyz, prints the URL and the operator-bypass hint, writes a pid file; `stop`, `status`, `reset` (wipe state) subcommands; several instances on one machine never collide; nothing it writes is committed. `just up`, `just down`, `just eph <cmd>` recipes; one README paragraph. Prove it: two worktrees' worth (simulate with two different dirs) up at once, both answer /readyz, both stopped, no residue.
4. `docs/stories/platform-admin-legs.md` — decompose the NEW/EXTEND requirements into bounded implementation legs (target 4–6 legs, ≤3 concurrent) with owned paths that do not overlap, dependencies, and a full brief per leg in the brief-template shape (WORKSPACE placeholder `~/dev/worktrees/rn-site--<leg>` branch `rb-<leg>`; READ FIRST; YOU OWN; WORK end states; ACCEPTANCE incl. `tools/ephemeral.sh` for self-testing; RAILS; REPORT). Note which legs depend on O1 landing. A final validation leg (Sonnet, human-style walkthrough + UX critique via agent-browser against an ephemeral instance) is dispatched by the orchestrator — write its brief too.

ACCEPTANCE: `bash -n tools/ephemeral.sh`; the two-instance proof; board builds with zero console errors (agent-browser console) and every frame viewed; requirements doc covers all 21 stories; legs doc has non-overlapping owned paths; `pgrep -f 'rn-site|http.server'` empty; no residue.
RAILS: production/deploy out of scope. Do not touch crates/** or templates/**. Never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units and `git push origin rb-x0` after each. `CARGO_BUILD_JOBS=6`. Do not spawn agents. Work autonomously; do not stop to ask questions. Do not kill processes you did not start (check PID command line and start time; O1's rn-site on 3004 is not yours).
REPORT: commit hashes; the requirement counts by EXISTS/EXTEND/NEW/DEFER; the leg list with owned paths and dependencies; the board URL recipe and screenshot paths; the ephemeral port scheme; blockers.
