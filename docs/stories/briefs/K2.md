WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--k2 (Rust workspace, branch `rb-k2`, worktree of ~/dev/love/projects/ronit/rn-site, branched from `rebuild` which already contains K1's kernel core). A sibling worker (K3, merge) edits crates/kernel at the same time in another worktree: it owns `crates/kernel/src/merge/**`, `crates/kernel/src/observe/**` (extensions), `migrations/3_merge.sql`. You own everything else you create below; touch shared files (`lib.rs`, `Cargo.toml`, `cmd/mod.rs`) only with additive lines.

READ FIRST, in order: docs/rebuild/plan.md (§Model relation vocabulary, ownership, statuses; §Gate manifest rows "no full scan", "kernel contract"); docs/kernel/index.html §One relation store, §Principal, §Resource registry, §Products plug in, §Schema, §check(), §Performance gates; crates/kernel/src (K1's store/`Sql` trait, ids, cmd pattern, principal, feed, test harness — follow its conventions exactly); crates/api/src (command args for CreateOrganization, CreateGroup, Invite, ClaimLink, SetRole, Leave, Share, Revoke, Transfer, CreateDocument, EditDocument, PublishDocument). That is all the context you get.

YOU OWN: crates/kernel/src/{relation,resource,org,group,invite,document}/**, `crates/kernel/migrations/2_relations.sql` (if K1 put migrations elsewhere, follow K1's layout), crates/kernel/benches/{check,list}.rs, the new cmd modules. You may READ everything.

WORK — leg K2 relations. End states:
1. Schema (new migration only): anything the report's schema needs that K1 did not create for `resource`, `relation`, `membership`, and the 1:1 kind table `document { resource_id PK, title, body, published_rev, draft_rev }`; indexes `relation(object_kind, object_id)`, `relation(subject_kind, subject_id)`, UNIQUE(object, relation, subject); `membership` PK (group_id, party_id); CHECK on role vocabularies. Organizations and groups are `party` rows (kind organization|group) — an organization's members are a membership on the org's own party row (the org is also its root group); a group row records its owning org or person in `party.owner_party_id`? — NO: groups never own and the report keeps ownership in `resource`. Model groups as: `party(kind=group)` + `resource(kind='group', owner_party_id=person|org)`; same for organizations (`resource(kind='organization', owner_party_id = founding person)`, ownership transferable). Document this in the module doc.
2. Relation store: `relation::grant/revoke/list_for_object/list_for_subject`, vocabulary registry in code (`Vocabulary { kind -> admitted relations -> admitted subject kinds }`; a `patient`-style example test that a kind refusing `@public` cannot be granted it). Nesting in code: `viewer<commenter<editor`, `member<admin<owner`. `check(principal, relation, object) -> bool` = one indexed query over the expanded subject set (K1's `SubjectSet`, now filled with orgs + groups via membership) + nesting. `list_visible(principal, kind, page) -> Vec<resource>` = one query joining ownership ∪ relations, paginated by (created_at, id).
3. Commands: CreateOrganization (party+resource+owner membership+audit), CreateGroup (in an org or personal; creator = owner membership), Invite (link with `{group, role, expires}` payload; bearer), ClaimLink (bearer principal + signed-in identity → membership; single use; expiry), SetRole (owner/admin only; owner cannot demote the last owner), Leave, Share (object #relation @subject; only editor-or-owner may share; vocabulary enforced), Revoke, Transfer (owner → new owner person|org; the only way owner_party_id changes; groups refused as owner), CreateDocument (resource+document, owner = acting_as), EditDocument (draft_rev bump with expected-rev guard — the client's version tag), PublishDocument (state transition). Contact scoping: `Share` with relation `contact` on a person object to a group subject is how contact info is visible inside a group and nowhere else; a `visible_contacts(principal, group)` query proves it.
4. Tests: every command; proptests: check nesting monotonic, groups never owner, transfer is the only owner change, no relation outside the vocabulary, cross-group contact leak impossible (random groups/persons/shares → visible_contacts never includes a person outside a shared group); `explain_*` for check, list_visible, membership expansion, visible_contacts.
5. Benches (criterion): `check` cold/warm at 10⁶ relations with adversarial shapes (one subject in 10⁴ groups; one object with 10⁴ grants); `list_visible` at 10⁴ objects. Budgets: check ≤300 µs cold / 20 µs warm; list ≤5 ms. Report the numbers; if a budget fails, fix the query/index, do not relax the budget.

OUT OF SCOPE: merge/match/person_link/alias (K3), server, UI, events/photos products.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-kernel --all-targets -- -D warnings`; `cargo test -p rn-kernel`; `cargo bench -p rn-kernel --bench check -- --quick` and `--bench list`; `tools/size-gate.sh`; no `unwrap()` outside tests; no test residue.
RAILS: never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-k2` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, test names (proptests + explain), bench numbers vs budget, the exact modelling of org/group ownership you landed, any shared-file lines you added, blockers.
