WORKSPACE: /Users/ronitnath/dev/worktrees/rn-site--k3 (Rust workspace, branch `rb-k3`, worktree of ~/dev/love/projects/ronit/rn-site, branched from `rebuild` which already contains K1's kernel core). A sibling worker (K2, relations) edits crates/kernel at the same time in another worktree: it owns `crates/kernel/src/{relation,resource,org,group,invite,document}/**` and `migrations/2_relations.sql`. Touch shared files (`lib.rs`, `Cargo.toml`, `cmd/mod.rs`) only with additive lines. Where merge must rewrite memberships/relations (tables K2 creates), write against the report's schema for those two tables (membership(group_id, party_id, role); relation(object_kind, object_id, relation, subject_kind, subject_id, granted_by, at)) — the orchestrator merges both legs and reconciles names.

READ FIRST, in order: docs/rebuild/plan.md (§Model; §Product contract commands ProposeMatch, ConfirmMatch, RuleMatch, Split); docs/kernel/index.html §Identity, §Merge is first class, §Merge(absorbed → survivor), §Two write lanes, §Schema (person_link, person_alias, match_candidate), §Performance gates (merge ≤50 ms at 10⁴ rows); crates/kernel/src (K1's store/`Sql` trait, ids, cmd pattern, principal, feed, observe, test harness — follow its conventions); crates/api/src (the four command args). That is all the context you get.

YOU OWN: crates/kernel/src/merge/**, crates/kernel/src/observe/** (extend K1's drain with match scanning), `migrations/3_merge.sql`, crates/kernel/benches/merge.rs. You may READ everything.

WORK — leg K3 merge. End states:
1. Schema (new migration only): whatever the report's `person_link` (append-only; enforce with a trigger that refuses UPDATE/DELETE), `person_alias`, `match_candidate` need beyond K1; indexes for alias lookup and candidate-by-identity.
2. Signals → candidates (observation lane, async): after Register/VerifyEmail/AddFactor events, a scan proposes `match_candidate` rows for identities sharing a verified email/phone factor value, the same OIDC subject, or name + a shared group. Score per signal; a candidate is only ever `proposed` by a signal — a proptest proves no signal path can write `confirmed`.
3. Proof → merge: `ConfirmMatch` (self-link: the caller holds live sessions on BOTH identities in the candidate — check by session tokens supplied in args, or a shared verified factor); `RuleMatch` (platform operator with a non-empty evidence string; the audit row carries it); both run `merge(absorbed_person → survivor_person)` in ONE txn exactly as the report's five steps: person_link rows, memberships union strongest-role-wins, relations with subject person:absorbed → survivor union strongest-wins, person_alias, absorbed party status `merged`, audit + event `person.merged`. Sessions untouched (they bind identities). Older person survives. Identity with `person_id NULL` (unresolved) → link directly. `Split` is the inverse with a fresh person for a chosen identity set. `resolve_person(public_id)` follows aliases (old URLs keep working).
4. Recovery: from a merged person, `RotateFactor` on any of its identities is allowed (documented as the "lost account" path; use K1's AddFactor/RemoveFactor authorized by person-level ownership).
5. Tests: the five merge steps individually and together; idempotence (merge twice = once); proptests: merge preserves the union of access (for random access graphs, every relation reachable before is reachable after via the survivor), split ∘ merge restores membership sets, weak signal alone never merges, product tables never reference person_id (a schema test scanning `PRAGMA foreign_key_list` for every table); `explain_*` for alias resolution and candidate lookup.
6. Bench (criterion): merge at 10⁴ affected rows (memberships + relations) — budget ≤50 ms; report the number.

OUT OF SCOPE: relations vocabulary/check/list (K2), server, UI.

ACCEPTANCE: `cargo fmt --check`; `cargo clippy -p rn-kernel --all-targets -- -D warnings`; `cargo test -p rn-kernel`; `cargo bench -p rn-kernel --bench merge -- --quick`; `tools/size-gate.sh`; no `unwrap()` outside tests; no test residue.
RAILS: never print or commit secrets. Stage explicit files; never `git add -A`. Commit in units, `git push origin rb-k3` after each. `CARGO_BUILD_JOBS=4`. Do not spawn agents. Work autonomously; do not stop to ask questions.
REPORT: commit hashes, test names, bench number vs budget, the shared-file lines you added, the exact table/column names you assumed from K2's domain, blockers.
