# rn-site

Rebuild in progress (branch `rebuild`, started 2026-08-26). The legacy
Leptos-islands SSR application has been deleted; nothing from it survives.

The binding contract for this rebuild is `docs/rebuild/plan.md`: workspace
layout, the product contract, the API shape, the gate manifest and the ladder
of bounded legs. Read it first.

Supporting documents: `docs/kernel/index.html` (the ratified model — schema,
`check()`, surfaces, placement, performance gates), `docs/model-board.tldr`
(the diagrams) and `docs/design.md` (the design authority).

This file is rewritten once the rebuild has something to describe.
