# Ronit Nath legacy index

The retired Forgejo histories were preserved on 2026-08-28 before the service was decommissioned.

- `legacy/ronitnath/<branch>`: the former `ronitnath/ronitnath` repository
- `legacy/rn-site/<branch>`: the former `ronitnath/rn-site` repository
- `legacy/portfolio/<branch>`: the former `ronitnath/portfolio` repository
- `holding/static-20260828`: the static continuity site deployed during the rebuild
- Tags use the equivalent `legacy/<source>/<tag>` namespace.

New product work belongs on the normal default branch. These namespaced refs are archival and should not be force-pushed.
