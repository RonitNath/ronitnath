name: deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

env:
  LOCAL_IMAGE: 100.88.31.199:5000/ronitnath/ronitnath
  GHCR_IMAGE: ghcr.io/ronitnath/ronitnath

jobs:
  build:
    runs-on: [self-hosted, delenda]
    permissions:
      contents: read
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
    steps:
      - uses: actions/checkout@v6

      - id: meta
        run: echo "tag=${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"

      - name: Build image
        env:
          CONTAINER_HOST: unix:///run/podman/podman.sock
        run: |
          podman --remote build \
            -f docker/ronitnath.Dockerfile \
            -t "$LOCAL_IMAGE:${{ steps.meta.outputs.tag }}" \
            -t "$LOCAL_IMAGE:latest" \
            -t "$GHCR_IMAGE:${{ steps.meta.outputs.tag }}" \
            -t "$GHCR_IMAGE:latest" \
            .

      - name: Push to local registry (deploy critical path)
        env:
          CONTAINER_HOST: unix:///run/podman/podman.sock
        run: |
          podman --remote push --tls-verify=false "$LOCAL_IMAGE:${{ steps.meta.outputs.tag }}"
          podman --remote push --tls-verify=false "$LOCAL_IMAGE:latest"

  deploy:
    runs-on: [self-hosted, nexus-deploy]
    needs: build
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - name: Drop legacy container if unmanaged
        run: |
          if docker inspect ronitnath >/dev/null 2>&1; then
            managed=$(docker inspect ronitnath --format '{{ index .Config.Labels "com.docker.compose.project" }}' || true)
            if [ -z "$managed" ]; then
              echo "Removing pre-compose container"
              docker rm -f ronitnath
            fi
          fi

      - name: Deploy
        working-directory: deploy
        run: |
          docker compose pull
          docker compose up -d --remove-orphans

      - name: Health check
        run: |
          for i in $(seq 1 30); do
            if curl -fsS -o /dev/null http://127.0.0.1:18080/; then
              echo "ok after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "health check failed"
          docker logs --tail 100 ronitnath || true
          exit 1

  ghcr-backup:
    runs-on: [self-hosted, delenda]
    needs: build
    permissions:
      contents: read
      packages: write
    steps:
      - name: Login to GHCR
        env:
          CONTAINER_HOST: unix:///run/podman/podman.sock
        run: echo "${{ secrets.GITHUB_TOKEN }}" | podman --remote login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Push to GHCR
        env:
          CONTAINER_HOST: unix:///run/podman/podman.sock
        run: |
          podman --remote push "$GHCR_IMAGE:${{ needs.build.outputs.tag }}"
          podman --remote push "$GHCR_IMAGE:latest"
