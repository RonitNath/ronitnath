# The server mounts this bundle's assets under /app/pkg/ and serves the tier
# shell for every path beneath /app, so trunk builds and serves the same shapes.
[build]
public_url = "/app/pkg/"

[serve]
serve_base = "/app"
