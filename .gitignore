node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env
.env.local
coverage/
test-results/
playwright-report/
.playwright/
screenshots/
.mail/

# Python bytecode from the offline catalogue builders (tools/starcat).
__pycache__/

# Bulk sky build artifacts: the TAP caches and the detail dataset. Its
# checksum and stats are the record of the build, so they stay.
data/*
!data/sky_star_detail.sha256
!data/sky_star_detail.stats.json
